{
  "name": "card-game-restart",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "mud-cli": "./bin/mud-cli.js"
  },
  "scripts": {
    "test": "node --test",
    "dev": "python3 -m http.server 5173",
    "simulate": "node scripts/simulate-runs.js",
    "cli": "node bin/mud-cli.js"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
