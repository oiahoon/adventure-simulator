# 技术架构（归档说明）

本文件内容已过期，原先描述的是早期武侠版与多模块原型结构，和当前运行代码不一致。

## 当前有效架构

请以以下文件为准：

- `public/src/idle-mud.js`（唯一 Web 运行时引擎）
- `public/data/events/*.json`（事件数据包）
- `docs/architecture/deep-research-report.md`（目标蓝图）
- `docs/architecture/event-engine-gap-analysis.md`（当前差距审计）
- `docs/schema/event-pack-schema.md`（运行时 schema 子集）

## 说明

保留本文件仅用于历史追溯，不再作为开发依据。
